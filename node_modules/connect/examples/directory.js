
/**
 * Module dependencies.
 */

var connect = require('../index');
var app = connect();
var path = __dirname + '/../';

app.use(connect.directory(path, { icons: true }))
app.use(connect.static(path))
app.listen(3000);
